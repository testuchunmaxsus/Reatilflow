/**
 * OpenAPI generatsiya joyi — `make gen-client` bu faylni qayta yozadi.
 *
 * Hozir placeholder:
 *   make gen-client
 *     → npx openapi-typescript http://localhost:8000/openapi.json --output web/src/api/schema.ts
 *
 * Generatsiya qilinganda bu fayl `openapi-typescript` tomonidan
 * to'liq OpenAPI sxemasi TS interfeyslariga aylantiriladi.
 * types.ts dagi qo'lda yozilgan tiplar shu fayl bilan to'ldiriladi/almashtiriladi.
 */

// Placeholder — generatsiya qilinguncha bo'sh
// eslint-disable-next-line @typescript-eslint/no-empty-object-type
export interface paths {}
// eslint-disable-next-line @typescript-eslint/no-empty-object-type
export interface components {}
