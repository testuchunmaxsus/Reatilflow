// Widget smoke test — RetailApp uchun asos test.
// Bu test asosiy app render qilishini tekshiradi.

import 'package:flutter_test/flutter_test.dart';

void main() {
  // Ilova integratsiya testlari alohida feature/test papkalarida.
  // Ushbu fayl placeholder sifatida saqlanadi.
  test('placeholder — widget_test', () {
    expect(1 + 1, equals(2));
  });
}
